# Windows batch and PowerShell scripts use CRLF on checkout.
*.bat   eol=crlf
*.ps1   eol=crlf
*.cmd   eol=crlf
# VBS launchers are UTF-16LE with BOM and contain their own CRLF pairs.
# Byte-based Git newline conversion would split UTF-16 code units.
*.vbs   -text
