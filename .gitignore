# 绝不提交任何机场的配置/订阅/凭据(可能含节点密码、订阅 token)
*.secure.yaml
verge.yaml
settings.yaml
subscription.yaml
credentials.json
remote_config.yaml
remote_config.json
profiles/
*.log
*.tmp

# 兜底层内核二进制(从你自己的 mihomo 内核复制, 不入库) + 临时文件
fallback/mihomo.exe
*.exe
fallback/_*.bat
