# ProxyClean 项目规则

- 日常只留 `00-打开 ProxyClean.vbs` 一个入口；旧入口目录用于兼容，不新增并排入口。Windows PowerShell 5.1 和 PowerShell 7 都要可用，VBS 编码由 `.gitattributes` 保持。
- 首页只有“检查并修复上网问题”和“关闭代理，恢复普通上网”；技术详情与维护工具放独立窗口。打开首页只读本机，不请求公网。后台检查保持界面响应，进度必须来自实际步骤。
- 普通修复先展示范围，确认后才改；只读检查可取消，写入或恢复中不能中断。只有一个已识别客户端时，一次关闭点击已包含正常退出及同一进程的有界残留处理，不再追加应用内确认；多个客户端先明确选择。系统提权须保留原用户、原选择和原进程身份。
- 客户端按实际进程关系及监听端口发现，不写死历史端口、不凭名称把独立核心归给另一客户端。关闭顺序为界面、核心、辅助进程；每次重核 PID、启动时间、路径、会话及端口归属，不追杀重启进程或复用端口。
- 只恢复本来运行、已识别的启动辅助服务，包括失败路径；不恢复独立代理核心，不改服务启动方式或依赖服务。恢复失败或核心重现时不能报关闭成功。
- 默认只清完全失效的本机手动代理；保留远端、存活、未知和混合配置的其他映射。指定端口操作只清该端点；客户端多个端口合成一轮可撤销修改。
- `-Direct` 仅覆盖手动 WinINET、用户 HTTP_PROXY/HTTPS_PROXY/ALL_PROXY、来源明确的通用全局 Git 代理及有健康物理回退的 IPv4 残留默认路由。PAC、WinHTTP、机器变量、NO_PROXY、Docker、其他应用和含糊 Git 来源不隐式改写。
- 路由仅改 ActiveStore，删除前重核物理回退及目标；IPv6 只处理明确的物理网卡，保留 Tailscale 等隧道。不用当前远程链路验收断网操作，不为修代理顺手重启网卡或换 DNS 服务器。
- DNS 缓存刷新只属于明确关闭客户端或显式缓存动作，普通修复不自动刷新。缓存失败、连续解析失败与连接失败分别报告；公网探测跳过显式代理不能证明绕过 TUN。
- 每轮效果前持久化 `%LOCALAPPDATA%\ProxyClean\last-operation.dpapi`，用 Windows 当前用户加密原值；预览不创建记录，修改前后分别比对和回读。失败逆序恢复，外部变化保留；未完成记录阻止新修改，不能删记录假装成功。撤销不复活进程，不恢复 DNS 缓存或网卡重置，记录也不是跨机备份。
- 设置回读、端口关闭、基础网页成功和所有应用直连是不同结论。保留的 PAC、隧道和应用独立代理必须说明；测试替身通过不能代替真实关闭、直连、重新启动验收。
- 诊断默认不落盘；只有显式请求才写日志或比较公网出口。公开诊断不带凭据、订阅、原始环境值、私有主机名、完整路径或 Docker 原始日志；恢复原值只进入恢复记录。
- 排查以当次客户端真实端点与失败步骤为准，不用旧端口、历史 DNS 配置或浏览器测试替代现场事实。本人已选择优先保障日常 AI 应用连通，不为浏览器泄露评分改坏可用的上网路径。

## 验证

已有 Pester 5.7.1 后，在两个 PowerShell 宿主分别运行 `ProxyClean.test.ps1`；`Test-WifiRebind.ps1` 是网络子集。测试使用隔离注册表、临时 Git 配置及替身，不关闭现用代理。
`ControlCenter.ps1 -SelfTest` 只构造窗口，不显示；`-SmokeTest` 会显示窗口，只有允许界面操作时才运行。零发现、未运行和跳过不算全套通过。
