mixed-port: 18099
allow-lan: false
mode: rule
log-level: warning
external-controller: 127.0.0.1:18199
# Retired reference only: no TUN, DNS hijack, static proxy-client upstream, or auto-start.
# If manually launched, this listener is DIRECT-only and must never be set as a persistent global proxy.

rules:
  - MATCH,DIRECT
